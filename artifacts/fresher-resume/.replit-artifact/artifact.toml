kind = "web"
previewPath = "/"
title = "Fresher Resume Builder"
version = "1.0.0"
id = "artifacts/fresher-resume"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 23736

[services.development]
run = "pnpm --filter @workspace/fresher-resume run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/fresher-resume", "run", "build" ]
publicDir = "artifacts/fresher-resume/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "23736"
BASE_PATH = "/"
